



/*

@Composable
fun TextFieldError(modifier: Modifier, textError: String) {
    Row(modifier = modifier) {
        Spacer(modifier = Modifier.preferredWidth(16.dp))
        Text(
            text = textError,
            modifier = modifier,
            style = currentTextStyle().copy(color = MaterialTheme.colors.error)
        )

    }
}*/
